{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Application",
            "type": "shell",
            // BUILD APPLICATION WITHOUT TESTING USING "-DskipTests"
            "command": "mvn clean package -DskipTests",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}